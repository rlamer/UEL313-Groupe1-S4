<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Watson{% endblock %}</title>
    <link rel="icon" href="{{ asset('images/logo-watson.png') }}" type="image/png">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    
    <link href="{{ asset('styles.css') }}" rel="stylesheet">
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_blog') }}">
                <!-- Logo plus grand -->
                <img src="{{ asset('images/logo-watson.png') }}" alt="Watson Logo" style="height: 100px;"> 
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_blog') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_article_index') }}">Articles</a>
                    </li>

                    <!-- Ajout du bloc de connexion/déconnexion -->
                    <li class="nav-item">
                        {% if app.user %}
                            <div class="mb-3">
                                Vous êtes connecté en tant que {{ app.user.userIdentifier }},
                                <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Se déconnecter</a>
                            </div>
                        {% else %}
                            <div class="mb-3">
                                <a href="{{ path('app_login') }}" class="btn btn-outline-primary">Se connecter</a>
                            </div>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Header -->
    <header class="bg-light py-5">
        <div class="container text-center">
            <h1>Bienvenue sur Watson</h1>
            <p>Ajoutez, partagez et découvrez des liens favoris en communauté</p>
        </div>
    </header>

    <!-- Contenu de la page -->
    <div class="container mt-4">
        {% block body %}{% endblock %}
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-white text-center py-4">
        <p>&copy; 2024 Watson</p>
        <p>UE L313 - Groupe 1</p>
    </footer>

    <!-- Script Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenUwT5gPp6o5Ah6gY3b5h0A6dZ3F5nEypOjf3fdy09tzTz7sdd09v0fNYq5Q+re" crossorigin="anonymous"></script>
</body>
</html>
